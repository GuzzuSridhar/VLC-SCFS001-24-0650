//  an mjs file with a single function
// default keyword is used when we have a single function in a .mjs
export default function add(a, b) {
  return a + b;
}

// alternatively
// export default add;
